using System;
using System.Linq;

namespace GaussAlgorithm
{
    public class Solver
    {
        public double[] Solve(double[][] matrix, double[] freeMembers)
        {
            var rowCount = matrix.GetLength(0);
            var colmnCount = matrix.GetLength(1);
            var buffer_matrix = (matrix).ToArray();
            for (int k = 0; k != rowCount; k++)
            {
                for (int )
            }
        }
    }
}
